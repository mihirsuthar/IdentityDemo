@model string
@{
    ViewBag.Title = "Create Role";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create Role</h2>

<br />
<div class="row col-md-12">


    @using (Html.BeginForm("Create", "RoleAdmin", FormMethod.Post, new { @class = "form-horizontal col-md-6" }))
    {
        @Html.ValidationSummary(true, "", new { @class = "alert alert-danger validation-errors" })

        @*if (ViewBag.Errors != null)
            {
                <span class="alert alert-danger">
                    @foreach (string error in ViewBag.Errors)
                    {
                        @error<br />
                    }
                </span><br /><br />
            }*@
        @Html.AntiForgeryToken()

        <div class="form-group">
            <label>Name</label>
            <input type="text" name="name" value="@Model" class="form-control" />
        </div>

        <div class="form-group">
            <button type="submit" class="btn btn-primary">Add Role</button>
            <a href="@Url.Action("Index", "RoleAdmin")" class="btn btn-danger">Cancel</a>
        </div>
    }
</div>